{
  "name": "node-context",
  "version": "0.4.0",
  "description": "Context type that carries deadlines, cancelation signals, and other request-scoped values.",
  "main": "lib",
  "devDependencies": {
    "istanbul": "^0.3.5",
    "jscs": "^1.11.3",
    "jshint": "^2.6.0",
    "mocha": "^2.1.0",
    "should": "^5.0.1",
    "sinon": "^1.12.2"
  },
  "scripts": {
    "cover": "istanbul cover _mocha -- --recursive && open coverage/lcov-report/index.html",
    "test": "jshint lib test && jscs lib test && istanbul cover _mocha -- --recursive --check-leaks && istanbul check-coverage --statements 100 --functions 100 --branches 100 --lines 100"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/silas/node-context.git"
  },
  "keywords": [
    "cancel",
    "context",
    "deadline",
    "expire",
    "timeout"
  ],
  "author": "Silas Sewell <silas@sewell.org>",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/silas/node-context/issues"
  },
  "homepage": "https://github.com/silas/node-context"
}
